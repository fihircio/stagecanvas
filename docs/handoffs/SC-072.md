Summary:
Added OSC/HTTP trigger stubs with register, fire, and event log endpoints.

Files touched:
- orchestration-server/app/main.py
- orchestration-server/app/models.py
- orchestration-server/tests/test_trigger_stub.py

Behavior change:
- Trigger rules can be registered and fired; events are stored in an in-memory log.

Contract impact: none

Sanity gate result: pass

How to test:
1) Run `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`.
2) Run `make sanity`.

Risks/assumptions:
- Trigger storage is in-memory only and resets on restart.

Next ticket suggestion:
- SC-073: persist trigger rules or integrate routing.
