Summary:
Implemented MVP preload/cache contract stubs across orchestration and render-node state.

Files touched:
- orchestration-server/app/models.py
- orchestration-server/app/main.py
- orchestration-server/app/registry.py
- orchestration-server/tests/test_preload_contract.py
- render-node/app/state.py
- render-node/tests/test_state.py
- orchestration-server/README.md
- render-node/README.md

Behavior change:
- Added additive API: `POST /api/v1/shows/preload`.
- Preload dispatch uses existing `LOAD_SHOW` command with additive payload fields:
  - `preload_only: true`
  - `assets: [...]`
  - `request_id`
- Heartbeat now supports optional `cache` object.
- Node snapshots (`GET /api/v1/nodes`) now expose cache state.
- Render-node `NodeState` now tracks cache fields and emits them via heartbeat/diagnostics on preload.

Contract impact: additive

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`
2) `python -m unittest discover -s render-node/tests -p 'test_*.py'`
3) `make sanity`

Risks/assumptions:
- Preload flow is an MVP stub; actual media transfer/caching remains mocked via state contract only.

Next ticket suggestion:
- SC-037 reconnect replay reliability integration tests.
