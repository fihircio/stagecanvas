Summary:
Added media ingest registry endpoints + models with idempotent asset registration.

Files touched:
- orchestration-server/app/models.py
- orchestration-server/app/registry.py
- orchestration-server/app/main.py
- orchestration-server/tests/test_media_registry.py
- docs/TICKETS.md

Behavior change:
- New media registry endpoints: POST/GET/PATCH `/api/v1/media` for asset metadata tracking.
- Asset IDs are idempotent; conflicting metadata returns 409 with reason code.

Contract impact: additive

Sanity gate result: not run (not requested)

How to test:
1) `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`

Risks/assumptions:
- Registry is in-memory; persistence would require a storage layer.
