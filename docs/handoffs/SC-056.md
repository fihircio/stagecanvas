Summary:
Propagated mapping config from show upsert into load_show dispatch and render-node bridge application, with validation and tests.

Files touched:
- orchestration-server/app/main.py
- orchestration-server/app/models.py
- orchestration-server/app/timeline_repository.py
- orchestration-server/tests/test_mapping_config_flow.py
- render-node/app/state.py
- render-node/app/bridge.py
- render-node/tests/test_mapping_config_flow.py
- render-node/README.md

Behavior change:
- Mapping config is stored with shows and injected into LOAD_SHOW payload when absent.
- Mapping config validation rejects invalid payloads with reason code.
- Render node applies mapping config via bridge before load_show.

Contract impact: none

Sanity gate result: pass

How to test:
1) Run `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`.
2) Run `python -m unittest discover -s render-node/tests -p 'test_*.py'`.
3) Run `make sanity`.

Risks/assumptions:
- Mapping config remains a stub; no real shader/mesh processing yet.

Next ticket suggestion:
- SC-060: playback stub tied to PLAY_AT timing.
