Summary:
Persisted render-node cache index to local JSON and restored on restart.

Files touched:
- render-node/app/state.py
- render-node/tests/test_cache_persistence.py
- render-node/README.md
- docs/TICKETS.md

Behavior change:
- Cache index now loads from and persists to a per-node JSON file.

Contract impact: none

Sanity gate result: pass

How to test:
1) `python -m unittest discover -s render-node/tests -p 'test_*.py'`
2) `make render-compile`
3) `make sanity`

Risks/assumptions:
- Persistence is local-only; no cross-node cache synchronization.
