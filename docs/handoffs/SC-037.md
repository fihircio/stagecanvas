Summary:
Added reconnect replay reliability integration tests for offline-queued command delivery behavior.

Files touched:
- orchestration-server/tests/test_reconnect_replay_reliability.py

Behavior change:
- New websocket+REST integration test verifies:
  - commands queued while node offline are replayed on reconnect,
  - reconnect without new commands does not replay old command,
  - subsequent command delivery order remains monotonic across reconnect cycles.

Contract impact: none

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`
2) `make sanity`

Risks/assumptions:
- Test validates replay behavior through current in-process websocket path.

Next ticket suggestion:
- SC-038: add per-node replay counters in operator snapshot for live reliability observability.
