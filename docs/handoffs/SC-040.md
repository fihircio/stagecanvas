Summary:
Completed SC-038, SC-039, and SC-040 with reliability counters in operator snapshots, reliability visibility in control UI, and preload cache contract v2 normalization.

Files touched:
- orchestration-server/app/models.py
- orchestration-server/app/main.py
- orchestration-server/app/registry.py
- orchestration-server/tests/test_operator_snapshot_reliability.py
- orchestration-server/tests/test_preload_contract_v2.py
- control-ui/lib/types.ts
- control-ui/components/nodes-dashboard.tsx
- control-ui/app/globals.css
- docs/TICKETS.md

Behavior change:
- Operator snapshot node entries now expose per-node reliability counters:
  - `replay_count`, `queued_count`, `reconnect_count`, `queue_depth`.
- Registry tracks queue/replay/reconnect counters and includes them in `/api/v1/nodes` and `/ws/operators` snapshots.
- Preload cache contract upgraded to v2 states and progress fields:
  - states normalized to `EMPTY/LOADING/READY/FAILED`
  - fields include `progress_assets_pct`, `progress_bytes_pct`, `progress_message`
  - legacy cache states (`IDLE/PRELOADING/ERROR`) are accepted and normalized.
- Control UI now shows reliability totals and per-node queue depth/replay/reconnect indicators.

Contract impact: additive (backward compatible)

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `cd orchestration-server && python -m unittest discover -s tests -p 'test_*.py'`
2) `cd control-ui && npm run lint && npm run build`
3) `make sanity`

Risks/assumptions:
- Reliability counters are in-memory per process (reset on restart) in current MVP architecture.
- UI treats missing counters as zero for compatibility.

Next ticket suggestion:
- SC-041: persist reliability counters in orchestration DB and expose time-window deltas.
