Summary:
Added time sync abstraction stub for selecting system/NTP/PTP and reporting drift through heartbeat.

Files touched:
- orchestration-server/app/models.py
- orchestration-server/app/registry.py
- render-node/app/agent.py
- render-node/tests/test_time_sync_stub.py

Behavior change:
- Render-node can select time sync source and report drift offsets via heartbeat.
- Orchestration records time sync source and uses it to compute drift levels.

Contract impact: none

Sanity gate result: pass

How to test:
1) Run `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`.
2) Run `python -m unittest discover -s render-node/tests -p 'test_*.py'`.
3) Run `make sanity`.

Risks/assumptions:
- NTP/PTP offsets are stub defaults; no real synchronization yet.

Next ticket suggestion:
- SC-073: integrate real time sync source.
