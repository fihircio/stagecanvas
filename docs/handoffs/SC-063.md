Summary:
Implemented media transfer worker v1 to copy local media into render-node cache and update cache progress.

Files touched:
- orchestration-server/app/registry.py
- orchestration-server/app/main.py
- orchestration-server/tests/test_media_transfer_worker.py

Behavior change:
- Transfer worker resolves media file paths and copies into per-node cache directories.
- Cache progress fields update based on actual bytes transferred.

Contract impact: none

Sanity gate result: pass

How to test:
1) Run `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`.
2) Run `python -m unittest discover -s render-node/tests -p 'test_*.py'`.
3) Run `make sanity`.

Risks/assumptions:
- Cache location is local (`render-node/data/cache/`) for dev only.

Next ticket suggestion:
- SC-064: render-node cache persistence.
