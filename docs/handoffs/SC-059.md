Summary:
Added mapping config load/save UI stub with orchestration validation feedback.

Files touched:
- control-ui/components/nodes-dashboard.tsx
- control-ui/app/globals.css
- control-ui/lib/types.ts
- docs/TICKETS.md

Behavior change:
- UI panel allows loading a sample mapping config JSON and saving via timeline show update endpoint.
- Validation errors from orchestration surface in-panel.

Contract impact: none

Sanity gate result: not run (not requested)

How to test:
1) `cd control-ui && npm run lint && npm run build`

Risks/assumptions:
- Mapping config persistence is handled by orchestration; UI does not yet fetch saved configs.
