Summary:
Added media upload endpoint that stores files locally and registers asset metadata.

Files touched:
- orchestration-server/app/main.py
- orchestration-server/app/models.py
- orchestration-server/app/registry.py
- orchestration-server/tests/test_media_upload.py

Behavior change:
- `POST /api/v1/media/upload` accepts multipart uploads, stores files under `orchestration-server/data/media/`.
- Media registry entry created with filename label, size, checksum, and file URI.

Contract impact: none

Sanity gate result: pass

How to test:
1) Run `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`.
2) Run `make sanity`.

Risks/assumptions:
- Local filesystem storage only; no object storage integration.

Next ticket suggestion:
- SC-063: media transfer worker v1.
