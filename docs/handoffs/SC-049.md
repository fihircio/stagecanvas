Summary:
Validated render-node bridge integration path with call-order and error propagation tests.

Files touched:
- render-node/tests/test_bridge_integration.py
- render-node/README.md
- docs/TICKETS.md

Behavior change:
- Bridge interface errors now have explicit test coverage (node status flips to ERROR).
- Mock bridge test verifies LOAD_SHOW then PLAY_AT call order.

Contract impact: none

Sanity gate result: not run (not requested)

How to test:
1) `python -m unittest discover -s render-node/tests -p 'test_*.py'`

Risks/assumptions:
- Tests use in-memory bridge; production bridge must raise exceptions on failures to propagate error state.
