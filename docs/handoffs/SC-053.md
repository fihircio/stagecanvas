Summary:
Added filesystem-backed media registry persistence stub and reload behavior for media assets.

Files touched:
- orchestration-server/app/registry.py
- orchestration-server/app/main.py
- orchestration-server/tests/test_media_storage_stub.py

Behavior change:
- Media registry writes asset metadata to a JSON file and reloads on startup.
- Orchestration server initializes media registry with local storage path.

Contract impact: none

Sanity gate result: pass

How to test:
1) Run `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`.
2) Run `make sanity`.

Risks/assumptions:
- Persistence is a local stub; no object storage integration yet.

Next ticket suggestion:
- SC-054: async transfer worker stub with retry/backoff.
