Summary:
Updated operator dashboard command flow to include `request_id` on all command posts and show richer command outcomes.

Files touched:
- control-ui/components/nodes-dashboard.tsx

Behavior change:
- Adds generated `request_id` per operator action.
- Sends `request_id` in command payloads to orchestration.
- Parses server response/error payload and surfaces reason-code-based details.
- Command result display now includes request id for traceability.

Contract impact: none

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `cd control-ui && npm run lint && npm run build`
2) Run UI and send commands; verify status line includes request id and reason details.

Risks/assumptions:
- UI generates request IDs client-side; replay behavior depends on server idempotency windows.

Next ticket suggestion:
- SC-007 timeline repository reliability tests.
