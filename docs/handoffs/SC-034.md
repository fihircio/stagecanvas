Summary:
Completed SC-033 and SC-034 with deterministic drift classification coverage in orchestration and WATCHNET-style drift alert surfaces in control UI.

Files touched:
- orchestration-server/app/registry.py
- orchestration-server/tests/test_drift_classification.py
- control-ui/components/nodes-dashboard.tsx
- control-ui/app/globals.css
- docs/TICKETS.md

Behavior change:
- Drift classification in orchestration snapshots is now explicitly derived from `abs(drift_ms)` during snapshot/summary construction.
- Added tests validating boundary levels and negative drift handling.
- Control UI now shows drift level counts (`OK/WARN/CRITICAL`) in the node panel.
- Each node shows a severity badge for `drift_level`, and cards alert when level is non-OK.

Contract impact: none

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`
2) `cd control-ui && npm run lint && npm run build`
3) `make sanity`

Risks/assumptions:
- UI count cards prefer server `drift_slo` counts when available, else fallback to local node aggregation.

Next ticket suggestion:
- SC-035: add operator-side trend sparkline for drift per node using rolling WS snapshots.
