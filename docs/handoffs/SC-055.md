Summary:
Added render-node cache index stub with LRU eviction and tests.

Files touched:
- render-node/app/state.py
- render-node/tests/test_cache_index.py
- render-node/README.md
- docs/TICKETS.md

Behavior change:
- Cache index tracks asset size and last-access time; evicts least-recently-used when over capacity.

Contract impact: none

Sanity gate result: not run (not requested)

How to test:
1) `python -m unittest discover -s render-node/tests -p 'test_*.py'`

Risks/assumptions:
- Cache index is in-memory only; integration with real storage will be needed later.
