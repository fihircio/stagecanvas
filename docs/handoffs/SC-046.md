Summary:
Exposed per-node drift history summaries in operator WS snapshot for quick trend visibility.

Files touched:
- orchestration-server/app/registry.py
- orchestration-server/tests/test_operator_snapshot_reliability.py
- control-ui/lib/types.ts
- docs/TICKETS.md

Behavior change:
- Operator WS snapshot now includes per-node `drift_history_summary` with sample counts, max/avg drift, and recent values.

Contract impact: additive

Sanity gate result: not run (not requested)

How to test:
1) `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`

Risks/assumptions:
- Summary reflects in-memory drift history window (bounded by `DRIFT_HISTORY_MAXLEN`).
