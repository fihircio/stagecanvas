Summary:
Implemented asset transfer queue worker stub with retry/backoff and observable status fields on node snapshots.

Files touched:
- orchestration-server/app/registry.py
- orchestration-server/app/main.py
- orchestration-server/tests/test_asset_transfer_worker.py

Behavior change:
- Transfer requests enqueue tasks and a background loop processes them.
- Worker updates queue depth, transfer worker state, and cache progress fields.

Contract impact: none

Sanity gate result: pass

How to test:
1) Run `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`.
2) Run `make sanity`.

Risks/assumptions:
- Task handler is a stub (no actual file transfer).
- Worker loop cadence is fixed; tune as needed for production.

Next ticket suggestion:
- SC-056: mapping config end-to-end validation and bridge flow.
