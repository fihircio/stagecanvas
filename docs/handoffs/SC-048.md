Summary:
Added orchestration asset transfer stub and cache-status propagation, with render-node cache updates.

Files touched:
- orchestration-server/app/models.py
- orchestration-server/app/main.py
- orchestration-server/tests/test_asset_transfer_stub.py
- render-node/app/state.py
- render-node/tests/test_state.py
- orchestration-server/README.md

Behavior change:
- Added `POST /api/v1/assets/transfer` stub that emits per-asset transfer commands and updates node cache state to LOADING.
- Node snapshots surface transfer progress via cache fields.
- Render-node `LOAD_SHOW` with `transfer_only` updates cache to LOADING then READY with progress fields.

Contract impact: additive

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`
2) `python -m unittest discover -s render-node/tests -p 'test_*.py'`
3) `make sanity`

Risks/assumptions:
- Transfer is a stub; progress is simulated via heartbeat cache updates.

Next ticket suggestion:
- SC-050 warp/blend pipeline skeleton.
