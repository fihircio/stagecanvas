Summary:
Added a unified root quality gate command and documented it.

Files touched:
- Makefile
- README.md

Behavior change:
- New `make check` command runs:
  - orchestration tests
  - render-node tests
  - control-ui lint/build
- Root README now documents `make check`.

Contract impact: none

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `make check`
2) `make sanity`

Risks/assumptions:
- `make check` assumes dependencies are installed or installable in current environment.

Next ticket suggestion:
- Align integration merge with SC-010+ branch to avoid overlapping render-node files.
