Summary:
Implemented operator confidence improvements in control UI: explicit websocket connection states with reconnect behavior and visible last-command success/error feedback.

Files touched:
- control-ui/components/nodes-dashboard.tsx
- control-ui/app/globals.css
- control-ui/.eslintrc.json
- control-ui/package.json
- control-ui/package-lock.json
- orchestration-server/pyproject.toml
- docs/TICKETS.md

Behavior change:
- WS status now surfaces `connecting`, `reconnecting`, `connected`, `disconnected`, `error`.
- Operator actions now persist latest command outcome including status, detail, and timestamp.
- UI can recover websocket stream after disconnects.
- Shared sanity gate remains green even with additional `orchestration-server/data` directory.

Contract impact: none

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) Run `make sanity`.
2) In `control-ui`, run `npm run lint && npm run build`.
3) Start UI/server, trigger PLAY_AT/PAUSE/STOP and verify last command panel updates.
4) Stop orchestration briefly, verify WS badge transitions through disconnected/reconnecting and recovers to connected.

Risks/assumptions:
- `control-ui` now pins `next` and `eslint-config-next` to `14.2.35` to keep lint/build behavior deterministic across agents.

Next ticket suggestion:
- SC-002: finalize orchestration timeline/dispatch idempotency and return structured error payloads for operator actions.
