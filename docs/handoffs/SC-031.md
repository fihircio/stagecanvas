Summary:
Implemented warn-log backpressure for repeated render-node warning events with configurable rate limits.

Files touched:
- render-node/app/agent.py
- render-node/tests/test_agent.py
- render-node/README.md
- docs/TICKETS.md

Behavior change:
- Added per-event-key warn rate limiter with burst/window controls.
- Repeated warning events are suppressed after burst limit within window.
- On window rollover, a summary warning is emitted with suppressed count.
- Added runtime flags:
  - `--warn-rate-window-sec`
  - `--warn-rate-burst`
- Diagnostics snapshot now includes:
  - `warn_rate_window_sec`
  - `warn_rate_burst`
  - `warn_emitted_count`
  - `warn_suppressed_count`

Contract impact: none

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `make render-test`
2) `make render-compile`
3) `make sanity`

Risks/assumptions:
- Suppression is per process and per event key; long-lived process behavior depends on chosen window/burst values.

Next ticket suggestion:
- SC-033: expose warn-rate limiter state on-demand via dedicated diagnostics endpoint/event.
