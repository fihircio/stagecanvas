Summary:
Added render-node playback stub tied to PLAY_AT timing with deterministic pause/stop handling.

Files touched:
- render-node/app/state.py
- render-node/tests/test_playback_stub.py
- render-node/README.md

Behavior change:
- Playback ticks increment frame counters at a fixed interval during PLAYING.
- PLAY_AT schedules start time; PAUSE and STOP freeze/reset playback state.

Contract impact: none

Sanity gate result: pass

How to test:
1) Run `python -m unittest discover -s render-node/tests -p 'test_*.py'`.
2) Run `make render-compile`.
3) Run `make sanity`.

Risks/assumptions:
- Playback is simulated; no real decode/render pipeline yet.

Next ticket suggestion:
- SC-061+: integrate real decode pipeline or further playback metrics.
