Summary:
Playback stub now gates PLAY_AT on cached media presence with reason codes.

Files touched:
- render-node/app/state.py
- render-node/tests/test_playback_cache_gate.py
- render-node/README.md
- docs/TICKETS.md

Behavior change:
- PLAY_AT returns error in command history when required assets are missing from cache.

Contract impact: none

Sanity gate result: pass

How to test:
1) `python -m unittest discover -s render-node/tests -p 'test_*.py'`
2) `make render-compile`
3) `make sanity`

Risks/assumptions:
- Required assets are specified via `required_assets` in PLAY_AT payloads.
