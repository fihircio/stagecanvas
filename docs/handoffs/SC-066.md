Summary:
Added supported media codec profile metadata and validation for asset creation.

Files touched:
- shared-protocol/messages.v1.json
- orchestration-server/app/models.py
- orchestration-server/tests/test_media_profile_validation.py

Behavior change:
- Codec profiles normalized to supported set (`HAP`, `HAP-Q`, `ProRes4444`, `H264`).
- Unsupported profiles rejected with validation error.

Contract impact:
- Shared protocol now documents supported codec profiles.

Sanity gate result: pass

How to test:
1) Run `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`.
2) Run `make sanity`.

Risks/assumptions:
- Validation runs in orchestration models; UI/profile editor not yet implemented.

Next ticket suggestion:
- SC-067: extend media metadata flow to UI or node preload.
