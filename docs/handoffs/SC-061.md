Summary:
Added dark mode support with theme toggle and CSS variable overrides.

Files touched:
- control-ui/app/globals.css
- control-ui/components/nodes-dashboard.tsx
- control-ui/app/layout.tsx
- docs/handoffs/SC-061.md

Behavior change:
- UI supports light/dark themes with toggle and persisted preference.

Contract impact: none

Sanity gate result: pass

How to test:
1) `cd control-ui && npm run lint && npm run build`
2) `make sanity`

Risks/assumptions:
- Theme preference stored in localStorage; new sessions default to system preference.
