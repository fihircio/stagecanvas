Summary:
Added API-level integration tests for persistent idempotency replay/mismatch and sequence monotonicity.

Files touched:
- orchestration-server/tests/test_idempotency_integration.py

Behavior change:
- New tests validate:
  - duplicate `request_id` replay path does not redispatch
  - payload mismatch for same `request_id` returns `REQUEST_ID_PAYLOAD_MISMATCH`
  - sequence numbers remain monotonic after re-opening persistent ledger DB

Contract impact: none

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`

Risks/assumptions:
- Tests reset global app registry/ledger during execution; expected to run in isolated test process.

Next ticket suggestion:
- SC-006 UI request-id propagation and result reason-code visibility.
