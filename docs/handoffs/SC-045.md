Summary:
Implemented sustained drift alert policy with UI surfacing distinct from instantaneous drift.

Files touched:
- orchestration-server/app/config.py
- orchestration-server/app/registry.py
- orchestration-server/tests/test_sustained_drift_alerts.py
- control-ui/components/nodes-dashboard.tsx
- control-ui/lib/types.ts

Behavior change:
- Sustained WARN/CRITICAL alerts trigger only after configurable consecutive samples.
- Alerts clear after return to sustained OK.
- UI uses alert level for card highlighting and alert counters, distinct from raw drift level.

Contract impact: additive

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`
2) `cd control-ui && npm run lint && npm run build`
3) `make sanity`

Risks/assumptions:
- Alert policy uses consecutive heartbeat samples as the time base.

Next ticket suggestion:
- SC-046 expose drift history summaries in operator WS snapshot.
