Summary:
Implemented SC-032 with a one-command render-node end-to-end smoke gate integrated into root Make targets.

Files touched:
- scripts/render-smoke.sh
- Makefile
- render-node/README.md
- docs/TICKETS.md

Behavior change:
- Added `scripts/render-smoke.sh` to:
  1) install orchestration + render-node deps,
  2) start orchestration on isolated port,
  3) run bounded render-node session,
  4) validate diagnostics JSONL output.
- Added root target `make render-smoke`.
- Smoke artifacts now live under `.sanity-logs/render-smoke`.

Contract impact: none

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `make render-smoke`
2) `make render-test`
3) `make sanity`

Risks/assumptions:
- Smoke script uses local editable installs and launches temporary orchestration process on default port `19010` (override via `ORCH_PORT`).

Next ticket suggestion:
- SC-033: add optional smoke mode that dispatches `PLAY_AT`/`PAUSE`/`STOP` commands during bounded run and verifies command counters increase.
