Summary:
Added basic timeline editor actions (track/clip CRUD) to the control UI.

Files touched:
- control-ui/components/nodes-dashboard.tsx
- control-ui/app/globals.css

Behavior change:
- UI can create/update/delete tracks and clips via existing orchestration endpoints.
- Timeline refreshes immediately after mutations.
- Errors surface through existing command result panel with reason codes.

Contract impact: none

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `cd control-ui && npm run lint && npm run build`
2) `make sanity`
3) Use Timeline editor controls to add/delete tracks/clips and verify list updates.

Risks/assumptions:
- UI uses simple form inputs; drag/drop and snapping are out of scope.

Next ticket suggestion:
- SC-052 mapping config model + validation.
