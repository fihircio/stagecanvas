Summary:
Added operator preload action and per-node preload readiness visibility in the UI.

Files touched:
- control-ui/components/nodes-dashboard.tsx
- control-ui/lib/types.ts
- control-ui/app/globals.css

Behavior change:
- Operator can trigger `PRELOAD` for all nodes or selected targets.
- Node cards show cache lifecycle state and progress counters.
- Non-ready nodes are clearly flagged prior to playback actions.

Contract impact: none

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `cd control-ui && npm run lint && npm run build`
2) `make sanity`
3) Load UI, click PRELOAD; verify node cards update readiness state.

Risks/assumptions:
- Preload dispatch uses MVP stub contract with empty asset list unless filled later.

Next ticket suggestion:
- SC-044 drift history ring buffer + API.
