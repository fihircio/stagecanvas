Summary:
Completed SC-041, SC-042, and SC-043 with preload reliability tests, PLAY_AT preload gating, and operator preload controls + readiness UI.

Files touched:
- orchestration-server/app/main.py
- orchestration-server/app/models.py
- orchestration-server/tests/test_preload_reliability.py
- orchestration-server/tests/test_play_at_preload_gate.py
- control-ui/components/nodes-dashboard.tsx
- control-ui/lib/types.ts
- control-ui/app/globals.css
- docs/TICKETS.md

Behavior change:
- `PLAY_AT` now rejects with `PLAY_AT_PRELOAD_NOT_READY` if any target node is not READY for preload.
- Preload cache contract accepts v2 lifecycle states and progress fields at API boundary.
- Operator UI includes PRELOAD action and per-node cache readiness + progress indicators.
- Reliability tests cover multi-node preload success/partial failure and retry behavior.

Contract impact: additive and backward compatible

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `cd orchestration-server && python -m unittest discover -s tests -p 'test_*.py'`
2) `cd control-ui && npm run lint && npm run build`
3) `make sanity`

Risks/assumptions:
- Preload gating assumes cache state is reported by render nodes; nodes without cache will be blocked until READY.

Next ticket suggestion:
- SC-044: add operator UI surfacing of preload block reason and allow override per node.
