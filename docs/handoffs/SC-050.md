Summary:
Introduced warp/blend pipeline skeleton wiring via mapping config propagation to render bridge.

Files touched:
- render-node/app/bridge.py
- render-node/app/state.py
- render-node/tests/test_warp_blend_pipeline.py
- render-node/README.md
- docs/ARCHITECTURE.md

Behavior change:
- Added `set_mapping` hook to bridge interface (no-op by default).
- `LOAD_SHOW` payload can include `mapping_config`, stored in state and passed to bridge before load.
- Tests validate propagation and error handling (mapping errors mark node ERROR).

Contract impact: none

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `python -m unittest discover -s render-node/tests -p 'test_*.py'`
2) `make render-compile`
3) `make sanity`

Risks/assumptions:
- Mapping config is a stub payload; shader/mesh implementation is out of scope.

Next ticket suggestion:
- SC-051 timeline editor MVP controls in UI.
