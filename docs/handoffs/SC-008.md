Summary:
Added orchestration contract tests for actionable error/success reason-code behavior.

Files touched:
- orchestration-server/tests/test_operator_contracts.py

Behavior change:
- New tests validate:
  - `PLAY_AT` short lead-time rejection includes `PLAY_AT_LEAD_TIME_TOO_SHORT`
  - broadcast without targets returns `NO_TARGETS`
  - operator pause replay returns `DUPLICATE_REQUEST`

Contract impact: none

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`

Risks/assumptions:
- Tests isolate app globals; parallel test execution is not assumed.

Next ticket suggestion:
- SC-009 unified local quality gate command.
