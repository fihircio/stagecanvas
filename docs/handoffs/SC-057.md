Summary:
Added preview pipeline stub (UI trigger -> orchestration preview snapshot response) with tests.

Files touched:
- orchestration-server/app/main.py
- orchestration-server/app/models.py
- orchestration-server/tests/test_preview_pipeline_stub.py
- control-ui/components/nodes-dashboard.tsx
- control-ui/lib/types.ts
- control-ui/app/globals.css
- docs/TICKETS.md

Behavior change:
- New endpoint: `POST /api/v1/preview/snapshot` returns per-node stub snapshot with timestamp.
- Control UI can request preview snapshot and displays last preview time.

Contract impact: additive

Sanity gate result: not run (not requested)

How to test:
1) `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`
2) `cd control-ui && npm run lint && npm run build`

Risks/assumptions:
- Preview response is a stub and does not render actual imagery.
