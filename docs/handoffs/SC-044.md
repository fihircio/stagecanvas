Summary:
Added per-node drift history ring buffer and API endpoint for trend analysis.

Files touched:
- orchestration-server/app/config.py
- orchestration-server/app/registry.py
- orchestration-server/app/main.py
- orchestration-server/tests/test_drift_history.py
- orchestration-server/README.md

Behavior change:
- Drift history is captured per node in a bounded ring buffer.
- Added endpoint: `GET /api/v1/nodes/{node_id}/drift_history`.
- History max length configurable via `DRIFT_HISTORY_MAXLEN`.

Contract impact: additive

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`
2) `make sanity`

Risks/assumptions:
- History stored in-memory; persistence requires future storage layer.

Next ticket suggestion:
- SC-045 sustained drift alert policy.
