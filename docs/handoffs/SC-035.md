Summary:
Added a deterministic two-node sync integration proof for `PLAY_AT` under drift SLO.

Files touched:
- orchestration-server/tests/test_two_node_sync_proof.py

Behavior change:
- New integration test validates two nodes receive identical `PLAY_AT` target time/sequence.
- Test simulates post-start heartbeats and asserts aggregate drift remains in `OK` tier (< warn threshold).
- Covers MVP acceptance criterion in `docs/ARCHITECTURE.md` for two-node synchronized playback.

Contract impact: none

Sanity gate result: pass

Sanity artifacts:
- .sanity-logs/report.txt
- .sanity-logs/orchestration-dev.log
- .sanity-logs/control-ui-dev.log

How to test:
1) `python -m unittest discover -s orchestration-server/tests -p 'test_*.py'`
2) `make sanity`

Risks/assumptions:
- Drift proof uses deterministic heartbeat simulation in test harness, not hardware clock discipline.

Next ticket suggestion:
- SC-036 preload/cache contract stubs for Phase 1 media flow.
