# Shared Protocol

Canonical message contracts exchanged between:

- `control-ui`
- `orchestration-server`
- `render-node`

Current spec:

- `messages.v1.json`

Versioning rules:

- Additive fields only within same major version
- Breaking changes require new major schema file

